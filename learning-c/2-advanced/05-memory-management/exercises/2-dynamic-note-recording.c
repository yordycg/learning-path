/*
 * Dynamic Note Recording:
 *
 * Pseudocode:
 *
 * Reservar sizeof(int) de memoria. // 4byte
 * Definir variable contador. // saber cuantas notas almacenamos
 * Pedir al usuario que ingrese sus notas, indicarle
 * que para terminar debe ingresar '-1'.
 * Verificar el tamano de la memoria donde se ingresan
 * notas, y si esta llena debemos  aumentar (*2) la memoria.
 * Verificar que cuando ingrese '-1' el loop de
 * pregunta debe terminar.
 * Luego en una nueva variable float, calculamos el
 * promedio. // se divide por la variable contador
 * Mostrar promedio al usuario.
 *
 */
